# T01 handoff

Setup verification is in progress. Consult stages.json and the final setup receipt for the verified state.
The scientific application is not implemented; all 108 application gates remain pending.

Entry points: README for working commands; EXECUTION_GOAL.md for the later goal; stages.json for dependency order;
acceptance-matrix.json for the full gate inventory; DECISIONS.md for approved adaptations.
Modular docs/01–10 and SOURCES remain the acceptance contract.

Next implementation work after T01: the common oxygen/indexing/criteria engine (T02), then the resistance flow
provider (T02R). Source audits proceed independently. Do not begin the goal solely because this file exists.

The source archive and original fixture hashes are immutable. Generated source reports, runtime assets,
browser screenshots and test results belong in ignored output locations; retain compact evidence receipts
in this directory and CI artifacts. No deployment has occurred during setup.
